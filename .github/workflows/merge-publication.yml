name: Verify and publish preserved nested request commits
'on':
  push:
    branches: ['ops/rc6-merge-20260906']
permissions:
  contents: read
defaults:
  run:
    shell: bash
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        python: ['3.11', '3.13']
    steps:
      - uses: actions/checkout@v4
        with:
          repository: s7cret/pine2ast
          ref: 8627c94308f4cbd96b2a2ce8ad54e17530c312fd
          path: publisher
          sparse-checkout: .github/requests/restore.py
          sparse-checkout-cone-mode: false
          persist-credentials: false
      - run: |
          python - <<'PY'
          from pathlib import Path
          import os
          source = Path('publisher/.github/requests/restore.py').read_text()
          old = "STAGING = 'ops/rc6-requests-20260906'"
          assert source.count(old) == 1
          Path(os.environ['RUNNER_TEMP']+'/restore.py').write_text(source.replace(old,"STAGING = 'ops/rc6-merge-20260906'"))
          PY
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Restore exact original local commits
        run: |
          python - <<'PY'
          from pathlib import Path
          for path in Path('.github/merge').glob('*.patch'):
              path.write_bytes(path.read_bytes().replace(b'\n diff --git ', b'\ndiff --git '))
          PY
          python "$RUNNER_TEMP/restore.py" restore .github/merge/plan.json "$RUNNER_TEMP/evidence"
          cp "$RUNNER_TEMP/restore.py" "$RUNNER_TEMP/evidence/restore.py"
      - name: Install clean source and verification dependencies
        run: |
          python -m pip install setuptools==84.0.0 wheel==0.48.0 hatchling==1.32.0 build==1.6.0 pytest==9.1.1 ruff==0.16.6 tzdata==2026.2
          mkdir -p "$RUNNER_TEMP/source"
          git archive HEAD | tar -x -C "$RUNNER_TEMP/source"
          python -m pip install "$RUNNER_TEMP/source"
      - name: Full PineLib suite and changed-source lint
        env:
          PYTEST_DISABLE_PLUGIN_AUTOLOAD: '1'
        run: |
          python -m pytest -q --junitxml="$RUNNER_TEMP/evidence/pinelib.xml"
          python -m ruff check pinelib/abi/compiled_request.py pinelib/request/snapshots.py tests/test_compiled_nested_requests.py tests/test_snapshot_aliases.py
          python -m build --no-isolation --outdir "$RUNNER_TEMP/evidence/dist" "$RUNNER_TEMP/source"
          python -m pip freeze > "$RUNNER_TEMP/evidence/environment.txt"
          test -z "$(git status --porcelain --untracked-files=no)"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: merge-${{ matrix.python }}-${{ github.run_id }}
          path: ${{ runner.temp }}/evidence/
          retention-days: 30
          if-no-files-found: error
  publish:
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          name: merge-3.13-${{ github.run_id }}
          path: evidence
      - run: python evidence/restore.py publish evidence
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: merge-publication-${{ github.run_id }}
          path: evidence/
          retention-days: 30
